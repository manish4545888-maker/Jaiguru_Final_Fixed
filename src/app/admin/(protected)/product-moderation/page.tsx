import { requireAdmin } from "@/lib/dal";
import { getPendingVendorProducts } from "@/lib/vendor/product-actions";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { ProductModerationActions } from "@/components/admin/product-moderation-actions";
export default async function ProductModerationPage() { await requireAdmin(); const products = await getPendingVendorProducts(); return <main className="mx-auto flex max-w-6xl flex-col gap-6"><div><p className="text-sm text-muted-foreground">Marketplace operations</p><h1 className="text-3xl font-semibold">Product moderation</h1><p className="mt-2 text-muted-foreground">Review vendor catalog submissions before they appear publicly.</p></div>{products.length === 0 ? <Card><CardContent className="p-8 text-sm text-muted-foreground">No products are waiting for review.</CardContent></Card> : <div className="grid gap-4">{products.map((product) => <Card key={product.id}><CardHeader><CardTitle className="flex flex-wrap items-center justify-between gap-3"><span>{product.name}</span><Badge>{product.approvalStatus}</Badge></CardTitle><p className="text-sm text-muted-foreground">{product.vendor?.businessName ?? "Vendor"} · {product.category.name}</p></CardHeader><CardContent className="flex flex-col gap-4"><div className="grid gap-2 text-sm sm:grid-cols-3"><p><span className="text-muted-foreground">Price:</span> ₹{product.price.toString()}</p><p><span className="text-muted-foreground">Stock:</span> {product.quantity}</p><p><span className="text-muted-foreground">Images:</span> {product.gallery.length}</p></div><p className="text-sm leading-6 text-muted-foreground">{product.longDescription || product.shortDescription || "No description provided."}</p><ProductModerationActions productId={product.id} /></CardContent></Card>)}</div>}</main>; }
