"use client";
import { useState } from "react";

export function SupportWidget() { const [open, setOpen] = useState(false); const [message, setMessage] = useState(""); const [answer, setAnswer] = useState(""); const [pending, setPending] = useState(false); async function send() { if (!message.trim()) return; setPending(true); const response = await fetch("/api/support", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ message }) }); const data = await response.json(); setAnswer(data.answer); setPending(false); } return <div className="fixed bottom-5 right-5 z-40"><button onClick={() => setOpen(!open)} className="rounded-full bg-primary px-5 py-3 font-medium text-primary-foreground shadow-lg">Support</button>{open ? <section className="mb-3 w-80 rounded-xl border bg-card p-4 shadow-xl"><h2 className="font-semibold">How can we help?</h2>{answer ? <p className="mt-3 text-sm text-muted-foreground">{answer}</p> : null}<textarea value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Ask about an order or tender" className="mt-3 min-h-20 w-full rounded-md border bg-background p-2 text-sm" /><button onClick={send} disabled={pending} className="mt-3 rounded-md bg-primary px-3 py-2 text-sm font-medium text-primary-foreground">{pending ? "Checking..." : "Send"}</button></section> : null}</div>; }
